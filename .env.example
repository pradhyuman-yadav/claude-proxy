# ── Required ──────────────────────────────────────────────────────────────────
# Your Claude Code OAuth token.
# 1. Install Claude Code:  npm install -g @anthropic-ai/claude-code
# 2. Authenticate:         claude auth login
# 3. Copy token from:      ~/.claude/credentials.json  (Linux/Mac)
#                          %APPDATA%\Claude\credentials.json  (Windows)
# Token looks like:        sk-ant-oat01-...
CLAUDE_CODE_OAUTH_TOKEN=

# ── Gemini backend (optional) ─────────────────────────────────────────────────
# GEMINI_BACKEND selects how gemini-* models are served:
#   cli — Antigravity OAuth via CLIProxyAPI (subscription). Log in at /terminal.
#         WARNING: Google has suspended accounts using CLI/Antigravity OAuth
#         through third-party proxies. Use at your own risk.
#   api — Google's official OpenAI-compatible endpoint (ToS-clean, free tier).
#         Requires GEMINI_API_KEY from https://aistudio.google.com/apikey
#   off — disable Gemini.
# Default: api if GEMINI_API_KEY is set, else cli.
GEMINI_BACKEND=
GEMINI_API_KEY=

# ── Auth ─────────────────────────────────────────────────────────────────────
# Optional. Set to protect /v1/* and the dashboard with Bearer token auth.
# Leave empty to allow open access (useful on a private network).
# Generate one: openssl rand -hex 32
API_KEY=

# ── Port configuration ────────────────────────────────────────────────────────
# HOST_PORT: port exposed on the host machine (what clients connect to)
# PORT:      port inside the container (usually same as HOST_PORT)
HOST_PORT=3456
PORT=3456
